const FecthingData = (props) => {

    return (
    <div>
      <h1>{props.title}</h1>
      <img src={props.img} alt="hai" width={100}/>
      <h2>ini adalah data</h2>
      <p>{props.jamal}</p>
    </div>
  )
}

export default FecthingData